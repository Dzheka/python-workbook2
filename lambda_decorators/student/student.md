**Topic:** Students | **Concept:** Lambda

You have this list of students:
```python
students = [
    {"name": "Alice", "grade": 88},
    {"name": "Bob",   "grade": 72},
    {"name": "Carol", "grade": 95},
    {"name": "Dan",   "grade": 60},
]
```

Using **only lambda functions** (no `def`), define:
1. `sort_by_grade(students)` — returns list sorted by grade, highest first
2. `passing(students)` — returns students with grade >= 70
3. `get_names(students)` — returns list of names only

> Hint: use `sorted()`, `filter()`, `map()` together with your lambdas.

```python
# Your code here
sort_by_grade = ...
passing       = ...
get_names     = ...
```

---